// Merge this block into %USERPROFILE%\.config\opencode\opencode.json
// (create the file and the folder if they do not exist)
{
  "mcp": {
    "mem0": {
      "type": "remote",
      "url": "http://localhost:8090/mcp",
      "enabled": true
    }
  }
}
